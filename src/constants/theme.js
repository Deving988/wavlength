// Deliberately not Spotify-green — a warm, low-key palette that still
// reads clearly against a near-black background at a glance.
export const colors = {
  background: '#0E0E12',
  surface: '#17171D',
  surfaceAlt: '#1F1F27',
  accent: '#E8A33D',
  textPrimary: '#F5F5F7',
  textSecondary: '#8A8A94',
  border: '#26262E',
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export const typography = {
  display: { fontSize: 28, fontWeight: '700', letterSpacing: -0.5 },
  title: { fontSize: 20, fontWeight: '600' },
  body: { fontSize: 15, fontWeight: '400' },
  caption: { fontSize: 12, fontWeight: '500', letterSpacing: 0.4 },
};
